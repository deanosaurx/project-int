apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-mernapp.fullname" . }}
  labels:
    {{- include "helm-mernapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-mernapp.selectorLabels" . | nindent 4 }}
